build:
  settings:
    board_root: .
    dts_root: remap_lalapad_tdq
  cmake: remap_lalapad_tdq
  kconfig: remap_lalapad_tdq/Kconfig
#build:
#  cmake: .
#  kconfig: Kconfig
#  settings:
#    board_root: config
#    dts_root: .
