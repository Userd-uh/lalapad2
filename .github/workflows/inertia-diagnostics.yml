name: Build inertia diagnostics (left split3 only)
on:
  workflow_dispatch:
jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
    with:
      build_matrix_path: build-inertia-diagnostics.yaml
      archive_name: inertia-diagnostics
